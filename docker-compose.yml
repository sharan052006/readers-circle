services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: readerscircle
      POSTGRES_USER: readers
      POSTGRES_PASSWORD: readers
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

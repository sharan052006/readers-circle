spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/readerscircle}
    username: ${DB_USER:readers}
    password: ${DB_PASSWORD:readers}

app:
  jwt:
    secret: ${JWT_SECRET:local-development-secret-key-readers-circle-32chars}
